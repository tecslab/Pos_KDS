# Supabase client configuration (safe to expose to the browser once populated).
NEXT_PUBLIC_SUPABASE_URL=https://qsujkexjecpkcqawryqi.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_s15XpJvjCleci8HR_uAIeA_0ZgrIfoy

# Optional server-only configuration. Use only for a privileged server-side
# Supabase operation that cannot use the current user's access token. Store the
# real value in approved secret management; never commit or expose it to clients.
SUPABASE_SECRET_KEY=your-supabase-secret-key
